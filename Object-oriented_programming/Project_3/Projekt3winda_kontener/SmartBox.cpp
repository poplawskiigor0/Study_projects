#include "SmartBox.h"
#include<iostream>

/*template<typename T>
T SmartBox<T>::operator[](int number)
{
    Knot<T>* temp;
    temp=m_head+number-1;
    return temp->m_element;
}

template<typename T>
Knot<T>* SmartBox<T>::newpart()
{
    Knot<T> *ptr = new Knot<T>;
    ptr->m_next=nullptr;
    ptr->m_prev=nullptr;
    return ptr;
}

template<typename T>
void SmartBox<T>::add(T newpart){
    Knot<T>* temp;
    if(!*m_head)
    {
        *m_head=newpart();
        m_head->m_element=newpart;
    }
}

template<typename T>
SmartBox<T>::~SmartBox(){
   if(m_head) delete m_head;
   if(m_tail) delete m_tail;
}*/



/*template<typename T>
void SmartBox<T>::add(T newpart){
    T* newpart= new T*;
    m_lenght++;

}*/
